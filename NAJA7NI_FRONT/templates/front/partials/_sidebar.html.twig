        <!-- Sidebar -->
        <aside id="sidebar"
            class="w-24 bg-sidebar border-r border-sidebar-border h-screen flex flex-col items-center py-8 gap-8">
            <!-- Logo -->
            <div class="flex items-center justify-center mb-2">
                <img src="{{ asset('assets/images/logo-small.png') }}" alt="Naja7ni"
                    class="w-12 h-12 object-contain filter drop-shadow-md">
            </div>

            <!-- Navigation -->
            <nav class="flex flex-col gap-4 w-full px-2">
                <a href="{{ path('app_front') }}"
                    class="nav-item flex flex-col items-center gap-1 p-3 rounded-xl transition-all duration-200 group {{ app.request.attributes.get('_route') == 'app_front' ? 'active bg-primary/10 text-primary' : 'hover:bg-muted text-muted-foreground hover:text-foreground' }}">
                    <i data-lucide="home" class="w-6 h-6 transition-colors"></i>
                    <span class="text-[11px] font-medium">Accueil</span>
                </a>
                <a href="{{ path('app_profile') }}"
                    class="nav-item flex flex-col items-center gap-1 p-3 rounded-xl transition-all duration-200 group {{ app.request.attributes.get('_route') == 'app_profile' ? 'active bg-primary/10 text-primary' : 'hover:bg-muted text-muted-foreground hover:text-foreground' }}">
                    <i data-lucide="user" class="w-6 h-6 transition-colors"></i>
                    <span class="text-[11px] font-medium">Profil</span>
                </a>
                <a href="{{ path('app_front_categories') }}"
                    class="nav-item flex flex-col items-center gap-1 p-3 rounded-xl transition-all duration-200 group {{ app.request.attributes.get('_route') == 'app_front_categories' ? 'active bg-primary/10 text-primary' : 'hover:bg-muted text-muted-foreground hover:text-foreground' }}">
                    <i data-lucide="folder-open" class="w-6 h-6 transition-colors"></i>
                    <span class="text-[11px] font-medium">Catégories</span>
                </a>
                <a href="{{ path('app_front_meet') }}"
                    class="nav-item flex flex-col items-center gap-1 p-3 rounded-xl transition-all duration-200 group {{ app.request.attributes.get('_route') == 'app_front_meet' ? 'active bg-primary/10 text-primary' : 'hover:bg-muted text-muted-foreground hover:text-foreground' }}">
                    <i data-lucide="video" class="w-6 h-6 transition-colors"></i>
                    <span class="text-[11px] font-medium">Meet</span>
                </a>
                <a href="{{ path('app_front_jeux') }}"
                    class="nav-item flex flex-col items-center gap-1 p-3 rounded-xl transition-all duration-200 group {{ app.request.attributes.get('_route') == 'app_front_jeux' ? 'active bg-primary/10 text-primary' : 'hover:bg-muted text-muted-foreground hover:text-foreground' }}">
                    <i data-lucide="gamepad-2" class="w-6 h-6 transition-colors"></i>
                    <span class="text-[11px] font-medium">Jeux</span>
                </a>
                <a href="{{ path('app_front_events') }}"
                    class="nav-item flex flex-col items-center gap-1 p-3 rounded-xl transition-all duration-200 group {{ app.request.attributes.get('_route') == 'app_front_events' ? 'active bg-primary/10 text-primary' : 'hover:bg-muted text-muted-foreground hover:text-foreground' }}">
                    <i data-lucide="calendar" class="w-6 h-6 transition-colors"></i>
                    <span class="text-[11px] font-medium">Events</span>
                </a>
                <a href="{{ path('app_front_forums') }}"
                    class="nav-item flex flex-col items-center gap-1 p-3 rounded-xl transition-all duration-200 group {{ app.request.attributes.get('_route') == 'app_front_forums' ? 'active bg-primary/10 text-primary' : 'hover:bg-muted text-muted-foreground hover:text-foreground' }}">
                    <i data-lucide="messages-square" class="w-6 h-6 transition-colors"></i>
                    <span class="text-[11px] font-medium">Forums</span>
                </a>
            </nav>
        </aside>
