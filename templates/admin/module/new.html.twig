{% extends 'admin/base_admin.html.twig' %}

{% block title %}Create New Module - Admin{% endblock %}

{% block breadcrumb %}
    <li class="breadcrumb-item"><a href="{{ path('admin_dashboard') }}">Dashboard</a></li>
    <li class="breadcrumb-item"><a href="{{ path('app_module_index') }}">Modules</a></li>
    <li class="breadcrumb-item active">New Module</li>
{% endblock %}

{% block header_title %}Create New Module{% endblock %}

{% block body %}
    <div class="main-content">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <div class="card border-0 shadow-sm">
                    <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">Module Details</h5>
                        <a href="{{ path('app_module_index') }}" class="btn btn-light btn-sm">
                            <i class="feather-arrow-left me-1"></i> Back to List
                        </a>
                    </div>
                    <div class="card-body p-4">
                        {{ include('admin/module/_form.html.twig', {'button_label': 'Create Module'}) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
