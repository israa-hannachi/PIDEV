<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        h1 { color: #000; text-align: center; margin-bottom: 10px; }
        p { text-align: right; font-style: italic; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid #333; padding: 8px; text-align: center; }
        th { background-color: #f2f2f2; text-transform: uppercase; }
        tr:nth-child(even) { background-color: #f9f9f9; }
        .questions { text-align: left; margin-top: 10px; }
        .questions strong { color: #444; }
        .badge { display: inline-block; padding: 2px 6px; background: #28a745; color: #fff; border-radius: 4px; font-size: 12px; }
    </style>
</head>
<body>
    <h1>Liste des Jeux personnalisée</h1>
    <p>Document généré le {{ "now"|date("d/m/Y H:i") }}</p>

    <table>
        <thead>
            <tr>
                <th>Titre</th>
                <th>Type</th>
                <th>Niveau</th>
                <th>Score Max</th>
                <th>Durée</th>
                <th>Tentatives</th>
            </tr>
        </thead>
        <tbody>
            {% for game in games %}
                <tr>
                    <td>{{ game.titre }}</td>
                    <td>{{ game.type }}</td>
                    <td>{{ game.niveau }}</td>
                    <td>{{ game.scoreMax }}</td>
                    <td>{{ game.duration }}</td>
                    <td>{{ game.attemptNumber }}</td>
                </tr>
                <tr>
                    <td colspan="6" class="questions">
                        <strong>Questions :</strong>
                        <ul>
                            {% for question in game.questions %}
                                <li>
                                    {{ question.questionText }}
                                    <span class="badge">Correcte : {{ question.correctAnswer }}</span>
                                </li>
                            {% else %}
                                <li><em>Aucune question pour ce jeu.</em></li>
                            {% endfor %}
                        </ul>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="6">Aucun jeu trouvé</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>
