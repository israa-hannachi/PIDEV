<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Ticket - {{ event.titre }}</title>
    <style>
        body { font-family: 'Helvetica', sans-serif; color: #333; }
        .ticket {
            border: 2px dashed #6366f1;
            padding: 40px;
            max-width: 600px;
            margin: 0 auto;
            border-radius: 20px;
            position: relative;
        }
        .header {
            text-align: center;
            border-bottom: 2px solid #f1f5f9;
            padding-bottom: 20px;
            margin-bottom: 20px;
        }
        .logo { font-size: 24px; font-weight: bold; color: #6366f1; }
        .event-title { font-size: 22px; font-weight: bold; margin: 10px 0; }
        .details { margin-bottom: 30px; }
        .label { font-size: 10px; color: #64748b; text-transform: uppercase; font-weight: bold; margin-bottom: 2px; }
        .value { font-size: 16px; font-weight: bold; margin-bottom: 15px; }
        .footer {
            text-align: center;
            font-size: 12px;
            color: #94a3b8;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #f1f5f9;
        }
        .badge {
            background-color: #6366f1;
            color: white;
            padding: 10px 20px;
            border-radius: 10px;
            display: inline-block;
            font-weight: bold;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="ticket">
        <div class="header">
            <div class="logo">NAJA7NI</div>
            <div class="event-title">{{ event.titre }}</div>
        </div>

        <div class="details">
            <div class="label">Participant</div>
            <div class="value">{{ registration.visitorName }}</div>

            <div class="label">Email</div>
            <div class="value">{{ registration.visitorEmail }}</div>

            <div class="label">Date & Heure</div>
            <div class="value">{{ event.dateDebut|date('d F, Y @ H:i') }}</div>

            <div class="label">Lieu</div>
            <div class="value">{{ event.lieu }}</div>
        </div>

        <div class="header" style="border-top: 2px dashed #e2e8f0; border-bottom: none; padding-top: 20px;">
            <div class="label">Numéro de Ticket</div>
            <div class="value">#REG-{{ registration.id }}</div>
            <div class="badge">CONFIRMÉ</div>
        </div>

        <div class="footer">
            Merci pour votre inscription ! Veuillez présenter ce ticket (numérique ou imprimé) à l'entrée.
        </div>
    </div>
</body>
</html>
